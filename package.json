{
  "name": "hal-assistant-webapp-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker/*.test.js && node --check worker/index.js && wrangler types --check",
    "cf:dev": "wrangler dev",
    "cf:dry-run": "wrangler deploy --dry-run",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
